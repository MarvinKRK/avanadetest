This code is prepare to run on windows, but it can also run on other systems.

You need:
- Google Chrome v76, (you can try run this tests on other web browser, but you need driver to do it)
- Java 1.8
- Maven 3.6

To run please execute `mvn test`